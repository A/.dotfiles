---
# TODO: aur remove sudo password request on install
# TODO: vim explicit keybindings with autogenerated docs
# TODO: sync github
# TODO: fzf move to role and handle nvim config change
# TODO: ??? use set_fact to prepare all items need to be appended to zshrc before execution
#       otherwise it leads to missed blocks when zshrc is regenerated by zsh tag

- hosts: localhost
  tasks:
    - import_role: name=meta_pacman
      tags: [pacman]

    - import_role: name=rust
      tags: [rust]

    - import_role: name=aur
      tags: [aur]

    - import_role: name=git
      tags: [git]

    - import_role: name=zsh
      tags: [zsh]

    - import_role: name=tmux
      tags: [tmux]

    - import_role: name=ripgrep
      tags: [ripgrep]

    - import_role: name=python
      tags: [python]

    - import_role: name=yarn
      tags: [yarn]

    - import_role: name=nvim
      tags: [nvim]

    - import_role: name=docker
      tags: [docker]
