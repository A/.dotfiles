---
# DONE: nvim
# DONE: oh-my-zsh
# DONE: yarn (global n)
# DONE: git setup (username, email, ...)
# DONE: fzf, global arch packages
# DONE: tmux & config
# DONE: pip{2,3} global packages: jmespath
# DONE: rust & cargo
# DONE: tmux - install 3.2 from sources
# DONE: cargo spaceship
# DONE: aur
# TODO: aur remove sudo password request on install
# TODO: vim explicit keybindings with autogenerated docs
# TODO: sync github
# TODO: fzf move to role and handle nvim config change
# TODO: ??? use set_fact to prepare all items need to be appended to zshrc before execution
#       otherwise it leads to missed blocks when zshrc is regenerated by zsh tag

- hosts: localhost
  roles:
    - meta_pacman
  vars: 
    pacman_packages: "{{ pacman_global_packages }}"
  tags:
    - pacman_packages

- hosts: localhost
  roles:
    - aur
  tags:
    - aur

- hosts: localhost
  roles:
    - zsh
  tags:
    - zsh

- hosts: localhost
  roles:
    - tmux
  tags:
    - tmux

- hosts: localhost
  roles:
    - nodejs
  tags:
    - nodejs

- hosts: localhost
  roles:
    - nvim
  tags:
    - nvim

- hosts: localhost
  roles:
    - ripgrep
  tags:
    - ripgrep

- hosts: localhost
  roles:
    - git
  tags:
    - git

- hosts: localhost
  roles:
    - rust
  tags:
    - rust

- hosts: localhost
  roles:
    - python
  tags:
    - python

- hosts: localhost
  roles:
    - starship
  tags:
    - starship
